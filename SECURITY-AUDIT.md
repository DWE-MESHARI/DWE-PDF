# DWE PDF — Security Audit

تاريخ المراجعة: 13 سبتمبر 2026

## النتيجة المختصرة

النظام يعمل محلياً في المتصفح ولا يحتوي على Backend أو رفع ملفات. بعد الإصلاحات
المذكورة أدناه، لم يظهر في كود التطبيق طلب شبكة خارجي، وكل الملفات المشار إليها
من `index.html` موجودة داخل الحزمة.

هذا لا يعني ضماناً مطلقاً ضد عيوب في المتصفح أو في مكتبات الطرف الثالث. تشغيل
ملفات PDF/DOCX غير موثوقة يظل يستهلك موارد الجهاز، ولذلك توجد حدود للحجم
ويجب إبقاء المتصفح محدثاً.

## ما تم فحصه وتأكيده

- فحص بناء JavaScript عبر `node --check`.
- فحص صحة `web.config` بصيغة XML.
- تشغيل التطبيق داخل Chromium عبر خادم Python محلي.
- تحميل JavaScript وCSS والمكتبات من `127.0.0.1` فقط أثناء التشغيل؛ سجل خادم
  التطبيق لم يظهر طلباً خارجياً.
- التحقق من أن مراجع `src` و`href` في `index.html` محلية وموجودة.
- البحث عن `fetch` و`XMLHttpRequest` وWebSocket وEventSource و`sendBeacon` في
  كود التطبيق؛ لم تظهر في `app.js`.
- اختبار HTML خبيث يحتوي على `script` و`onerror` و`svg` ورابط خارجي؛ منظف Word
  أبقاها كنص/تنسيق آمن ولم يسمح بتنفيذها أو تحميلها.
- فحص وجود روابط رمزية أو ملفات تنفيذية غير متوقعة داخل الحزمة.

## الإصلاحات المنفذة

- استبدال `npx --yes serve` في `start-local-server.bat` بـ
  `py -m http.server` أو `python -m http.server`، لمنع تنزيل وتنفيذ حزمة من
  الإنترنت وقت التشغيل.
- إضافة `sanitizeDocumentHtml()` قبل عرض ناتج Mammoth أو وضعه في نافذة الطباعة.
  يتم حذف عناصر السكربت والإطارات والنماذج وSVG والسمات الحدثية، وإزالة روابط
  HTML والصور غير المحلية.
- إضافة حد 20 MB لملفات DOCX والصور، مع قبول PNG/JPEG/WebP فقط في أداة الصور.
- تحويل WebP إلى PNG داخل المتصفح قبل إدخاله في PDF.
- إلغاء `ObjectURL` القديمة لمعاينات الصور لمنع تراكمها في الذاكرة.
- عزل نافذة طباعة Word عن الصفحة الأصلية وإضافة CSP خاصة بها.
- إضافة `web.config` لرؤوس الحماية عند النشر عبر IIS.
- تحديث إصدار cache-busting إلى `dwe-security-1`.

## قيود ومخاطر متبقية

1. `pdf-encrypt-lite` يستخدم RC4-128، وهو تشفير قديم. لا ينبغي اعتباره حماية
   حديثة لملفات حساسة جداً؛ استخدم تشفيراً حديثاً خارج هذه الأداة عند الحاجة.
2. CSP الحالية تحتوي على `unsafe-inline` لأن الواجهة القديمة تستخدم event
   handlers داخل HTML مثل `onclick` و`onchange`. هذا يقلل من قوة CSP، لكنه لا
   يلغي التنقية المضافة لمحتوى Word. إزالة هذا الاستثناء تتطلب نقل handlers إلى
   `addEventListener` وإزالة inline styles تدريجياً.
3. مكتبات PDF.js وpdf-lib وJSZip وMammoth محلية، لكن أي عيب مستقبلي في إصداراتها
   يحتاج تحديثاً يدوياً ومراجعة مصدر موثوق. الفحص المحلي لا يثبت عدم وجود CVE
   غير معروفة.
4. خادم Python مناسب للتشغيل المحلي، لكنه ليس خادم إنتاج أو خدمة عامة. عند
   النشر العام يجب استخدام HTTPS وخادم يرسل رؤوس `web.config` المكافئة، وعدم
   السماح برفع الملفات إلى خادم التطبيق.